class colMapper(object):
	# Set map commands
	def setMapCmdWithString(self, stringCmd):
		return
	def pushMapCmd(IMapOper):
		return
	# Engine
	def mapTo(fSheet, tSheet, topRow=2, bottomRow=0, toTopRow=2):
		if bottomRow < 1:
			bottomRow = fSheet.UsedRange.Rows.Count
		assert topRow < bottomRow
		assert toTopRow > 0
		for row in range(topRow, bottomRow):
			for command in mapStack:
				
		return
	pass
	